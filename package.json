{
  "name": "multi-agent-ai-dev-assistant",
  "version": "1.0.0",
  "description": "Multi-Agent AI Developer Assistant - A sophisticated system for development support",
  "private": true,
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=server\" \"npm run dev --workspace=client\"",
    "build": "npm run build --workspace=server && npm run build --workspace=client",
    "test": "npm test --workspace=server && npm test --workspace=client",
    "lint": "npm run lint --workspace=server && npm run lint --workspace=client",
    "format": "npm run format --workspace=server && npm run format --workspace=client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
